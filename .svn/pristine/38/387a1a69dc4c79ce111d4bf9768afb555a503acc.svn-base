package com.augurit.agmobile.gzpssb.utils;

/**
 * Created by xiaoyu on 2018/4/12.
 */

public class RadioGroupUtil {
    public static void disableRadioGroup(android.widget.RadioGroup testRadioGroup) {
        for (int i = 0; i < testRadioGroup.getChildCount(); i++) {
            testRadioGroup.getChildAt(i).setEnabled(false);
        }
    }

    public static void enableRadioGroup(android.widget.RadioGroup testRadioGroup) {
        for (int i = 0; i < testRadioGroup.getChildCount(); i++) {
            testRadioGroup.getChildAt(i).setEnabled(true);
        }
    }
}
